import persion from '../models/persion';

const persionReducer = (state = persion, action) => {
    switch (action.type) {
        default:
            return state;
    }
}
export default persionReducer;